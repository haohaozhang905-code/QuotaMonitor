# QuotaMonitor 开发约束

本文件用于约束人和编程 Agent 在 QuotaMonitor 仓库中的协作方式。目标是让需求、实现、测试和真实应用验收能够互相追踪。

## 开始改动前

1. 先检查 `git status`，保留并识别已有修改，不覆盖与当前任务无关的工作。
2. 阅读 `docs/PRODUCT.md`、`specs/README.md`，以及当前改动对应的 `spec.md` 和 `plan.md`；涉及界面时同时阅读 `docs/DESIGN_SYSTEM.md`。
3. 涉及数据来源时阅读 `docs/DATA_SOURCES.md`；涉及架构、状态栏或应用身份时阅读 `docs/ARCHITECTURE.md` 和 `CONTRIBUTING.md`。
4. 需求尚未明确时，先补齐目标、范围、非目标和验收条件，再进入实现。

## 何时需要 Spec

满足任一条件的改动，应建立或更新功能级 Spec：

- 改变用户可见行为或核心操作路径；
- 跨越两个以上模块；
- 改变数据口径、缓存、持久化或迁移语义；
- 新增数据源、网络请求、权限或凭据读取；
- 修改提醒规则、去重或投递逻辑；
- 涉及状态栏、Bundle ID、登录项、安装或发布流程。

小范围文案、注释和不改变语义的局部样式修复，可直接在任务或 Pull Request 中说明。

## Spec 与 Plan 的职责

- `spec.md` 记录用户问题、目标、范围、非目标、需求编号、异常状态和可观察验收标准。
- `plan.md` 记录架构方案、数据流、迁移、文件改动、实施阶段、测试、发布与回滚。
- 已实现功能的事后基线必须标记 `provenance: reconstructed`，不能描述成开发前已经确认的文档。
- 被替代的规范标记为 `superseded` 并保留来源，不直接抹去决策历史。

## 数据与隐私边界

- 未确认字段语义时不得推估 Token、余额、费用或额度。
- 缺失、失败、扫描中、过期和沿用旧缓存的数据不能当作零，也不能触发新的风险事件。
- 只读取完成任务所需的最小本地数据，不提交凭据、认证文件、完整会话、用户名或精确位置。
- 新增远程服务、分析或遥测前，必须在 Spec 中说明目的、字段、授权、保留周期和降级方式。

## 保护边界

- 普通提醒、Token 或页面改动不得顺带修改状态栏创建、Bundle ID、登录项或安装脚本。
- 禁止设置 `NSStatusItem.autosaveName`。
- 应用名称、版本和 Bundle ID 统一从 `script/app_config.sh` 读取。
- 持久状态的语义变化必须提供向前迁移、旧数据兼容和回滚说明。

## 实现与验证

- 每项需求 ID 应在 `docs/TRACEABILITY.md` 中关联实现、测试和验收状态。
- 已复现缺陷应尽量转化为单元测试、Fixture、静态保护或运行检查。
- 数据源改动至少覆盖正常记录、缺失字段、重复记录、追加写入、临时不可读和缓存重载。
- 用户可见文案必须同步维护中英文资源。
- 视觉改动优先复用 `docs/DESIGN_SYSTEM.md` 和 `PanelTheme` 的语义 Token；改变公共 Token、信息层级或核心组件时同步更新设计规范。
- 运行 `./script/verify_sdd_docs.sh` 和与改动风险相称的测试。
- 构建和单元测试通过只能证明静态与逻辑检查；真实界面、系统通知、菜单栏和安装行为需要独立验收并记录结果。

## 完成汇报

完成时明确区分：

- 已实现；
- 自动化检查已通过；
- 真实应用已验证；
- 视觉验收已通过；
- 仍待验证或存在的风险。
