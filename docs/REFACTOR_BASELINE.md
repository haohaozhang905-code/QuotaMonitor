# 重构减量基线

本页记录提醒组件化与代码复用重构前的静态基线。行数按 Swift 文件物理行计数，包含注释与空行；重复块由 `script/code_metrics.rb` 统计跨文件重复的连续 8 行窗口，因此相邻窗口可能来自同一段较长重复实现，不能直接相加为可删除行数。

| 指标 | 重构前基线 |
| --- | ---: |
| 生产 Swift | 12,965 行 |
| Swift 测试 | 2,498 行 |
| `MainPanelView.swift` | 3,228 行 |
| `QuotaStore.swift` | 940 行 |
| 5 个本地 Token 客户端 | 2,202 行 |
| XCTest | 14 项通过 |
| Swift Testing | 94 项通过 |

重构后使用 `ruby script/code_metrics.rb` 复测。代码减量按生产 Swift 物理行差计算；新增的抽象、测试、注释不计作“删除重复代码”，同时以行为等价测试、缓存兼容和真实运行验收约束重构质量。

## 阶段性复测（2026-09-21）

| 指标 | 基线 | 当前 | 结果 |
| --- | ---: | ---: | --- |
| 生产 Swift | 12,965 行 | 12,967 行 | 比基线多 2 行；期间增加提醒 V5、用户离席门控、来源诊断和玻璃表面修订，不能把净行数当作纯重构减量 |
| Swift 测试 | 2,498 行 | 3,478 行 | 增加 980 行，覆盖规则迁移、投递竞态、用户状态、来源运行状态、快照兼容和扫描边界 |
| `MainPanelView.swift` | 3,228 行 | 570 行 | 单文件目标达成；页面与图表已拆分，拆分本身不计减量 |
| 页面与图表拆分文件 | — | 2,304 行 | `OverviewPageView`、`TokenPageView`、`SettingsPageView`、`TokenChartViews` 合计；用于描述职责迁移，不与基线比较 |
| `QuotaStore.swift` | 940 行 | 451 行 | 单文件目标达成；刷新、来源扫描编排与 Token 快照迁至 `QuotaStore+TokenSources.swift`（383 行） |
| 6 个本地 Token 客户端 | 2,202 行（原5个） | 1,888 行 | 当前统计含新增独立 Qoder 客户端，不与旧5文件集合直接比较 |
| Token 扫描公共支持 | — | 279 行 | 承载缓存容器、文件元数据、追加读取、默认缓存路径、单根目录扫描和 ISO-8601 日期解析；来源专用解析和 Codex fork / WorkBuddy generation 逻辑保留在各自 client |
| 提醒模型与协调器 | — | 897 行 | 规则目录、条件、通用文案描述、V5 账本与投递后提交；普通规则不再需要事件枚举或 Coordinator switch 分支 |
| 重复 8 行生产窗口 | 未记录 | 8 组 | 当前脚本分开统计生产与测试；窗口重叠，不能直接折算可删除行数，无可比原始基线 |
| XCTest / Swift Testing | 14 / 94 项 | 14 / 132 项 | 2026-09-21 当前全通过 |

阶段 1–4 已完成主要结构改造：提醒由 `ReminderRuleCatalog` 驱动并保留 V1～V4 到 V5 的顺序迁移；本地扫描公共件减少客户端缓存和目录代码；`QuotaStore` 使用来源状态字典、刷新任务描述与统一本地结果分支，并从 V2 快照迁移到 V3；来源安装探针、能力分组、平台图标和图表颜色索引集中声明；主面板拆为页面组件并共用图表交互状态。Qoder 的卸载后遗留日志例外有目录字段和测试覆盖，V2 Token 快照读取也有专用测试。

原定 5% 净减量目标没有达成：若仍沿用 12,316 行上限，当前相差 651 行。该阶段同时加入了提醒 V5、用户离席门控、来源诊断和新的视觉实现，因此净行数只作为历史指标。`MainPanelView.swift` 和 `QuotaStore.swift` 的单文件职责目标已经达成，拆分改善职责边界但不计作代码减量。2026-09-21 复测中，14 项 XCTest 与 132 项 Swift Testing 全部通过；文档门禁、源码安全、构建和真实界面状态分别以对应脚本及功能验收记录为准。
